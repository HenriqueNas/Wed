import '../../../wed.dart';

/// {@template div_props}
/// A [Component] that renders a `<div>` tag.
/// {@endtemplate}
class DivProps extends GlobalProps {
  /// {@macro div_props}
  const DivProps({
    super.id,
    super.title,
    super.styles,
    super.className,
  });
}
